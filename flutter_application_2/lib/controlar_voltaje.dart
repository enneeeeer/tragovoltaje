import 'package:flutter/material.dart';

class ControlarVoltaje extends StatelessWidget {
  const ControlarVoltaje({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Ventana para controlar voltaje',
        style: TextStyle(
          color: Colors.white,
        )
      ),
    );
  }
}
