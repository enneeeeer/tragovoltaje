import 'package:flutter/material.dart';

class ConectarPulsera extends StatelessWidget {
  const ConectarPulsera({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Ventana para conectar pulsera',
        style: TextStyle(
          color: Colors.white,
        )
      )
    );
  }
}
